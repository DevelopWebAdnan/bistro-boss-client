import SectionTitle from "../../../components/SectionTitle/SectionTitle";

const Payment = () => {
    return (
        <div>
            <SectionTitle heading="Payment" subHeading="Pay to eat"></SectionTitle>
            <h2 className="text-3xl">Taka uira uira asho</h2>
        </div>
    );
};

export default Payment;